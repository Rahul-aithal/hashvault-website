---
const { level, class: className = "", ...attrs } = Astro.props;
const Tag = `h${level}`;

const combinedAttrs = {
  ...attrs,
  class: `scroll-mt-48 ${className}`.trim(),
};
---

<Tag {...combinedAttrs}>
  <slot />
</Tag>
